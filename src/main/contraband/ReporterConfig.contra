package sbt.errorssummary
@target(Scala)

type ReporterConfig {
    colors: Boolean!
    shortenPaths: Boolean!
    columnNumbers: Boolean!
    errorColor: String!
    warningColor: String!
    infoColor: String!
    debugColor: String!
    sourcePathColor: String!
    errorIdColor: String!
}
